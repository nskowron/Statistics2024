image: registry.gitlab.com/uni901304/statistics2024
include: Exc4/.gitlab-ci.yml

stages:
  - build
  - test

build:
  stage: build
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make
  artifacts:
    paths:
      - build/bin